  getMinMax(a, b) {
    let r;
    if ('bar' == this.type) {
      r = this.getMinMaxForStackedBar(a, b);
    } else if ('area' == this.type) {
      r = {min: 0, max: 100};
    } else {
      r = this.getMinMaxElse(a, b);
    }
    return r;
  }
